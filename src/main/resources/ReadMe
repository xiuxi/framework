#############################################################
# 整合Angular注意事项                                          
#############################################################
Controller方法返回字符串必须先将字符串转为Json字符串再返回，或使用VJson包装返回(返回一定得是Json对象)

Controller所有异常交给ControllerUncaughtExceptionResolver处理,返回错误格式的Json对象,供前端解析(返回一定得是Json对象)




考虑在一个地方拦截Controller方法返回值然后统一包装返回给前端

#############################################################
#                                         
#############################################################

